[package]
name = "siftwave"
version = "0.1.0"
edition = "2024"

[dependencies]
rmcp = { version = "3.2.0", features = ["transport-io"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
